#include "opencv2/core/core.hpp"
#include "opencv2/highgui/highgui.hpp"
#include "opencv2/opencv.hpp"
#include "opencv2/videoio.hpp"
#include "opencv2/imgproc.hpp"

#include <iostream>

using namespace std;
using namespace cv;/*
int main()
{

    for(;;)
    {
          cout << "aqui";
          Mat frame;
          frame = imread("2644486.jpg");
          imshow("before", frame);
          //bilateralFilter(frame,frame,9,3);
       //   GaussianBlur(frame,frame,Size(51,51),0,0);
          //adaptiveThreshold();
          //dilate(frame,frame);
          //fastNlMeansDenoising(frame,frame,10,7,21);
          //if( frame.empty() ) break; // end of video stream
          //imshow("after", frame);
          if( waitKey(10) == 27 ) return 0; // stop capturing by pressing ESC

    }
    // the camera will be closed automatically upon exit
    // cap.close();
    return 0;
}
*/
